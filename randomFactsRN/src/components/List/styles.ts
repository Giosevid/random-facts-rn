import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  item: {
    marginBottom: 10,
  },
  text: {
    fontSize: 18,
    color: '#fff',
  },
});

export default styles;
